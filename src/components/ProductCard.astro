---
import type { Product } from "../types/product";

interface Props {
  product: Product;
}

const { product } = Astro.props;
---

<a
  href={product.amazonUrl}
  target="_blank"
  rel="nofollow"
  <div
  class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow"
>
  <img
    src={product.imageUrl}
    alt={product.title}
    class="w-full h-48 object-cover"
  />
  <div class="p-4">
    <h3 class="text-xl font-semibold mb-2 text-gray-800">{product.title}</h3>
    <p class="text-gray-600 mb-4">{product.description}</p>
    <div class="flex justify-between items-center">
      <span class="text-lg font-bold text-primary-dark">{product.price}€</span>
      <button
        class="bg-highlight hover:bg-highlight-dark text-gray-800 px-4 py-2 rounded transition-colors"
      >
        Ver en Amazon
      </button>
    </div>
  </div>
</a>
