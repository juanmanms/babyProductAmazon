---
import PostLayout from "../../layouts/PostLayout.astro";
import posts from "../../data/connectNotion";
export function getStaticPaths() {
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}
const { post } = Astro.props;
---

<PostLayout post={post} />
